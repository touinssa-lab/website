# 투어리즘 인사이트 홈페이지 개발 환경 및 핵심 서비스 안내서

본 문서는 투어리즘 인사이트(Tourism Insight) 홈페이지 리뉴얼 프로젝트의 전체적인 개발 구성 요소, 자동 배포 체계, 데이터베이스, 연동 API 및 핵심 자동화 기능에 대해 누구나 이해하기 쉽게 요약 정리한 안내서입니다.

---

## 1. 홈페이지 기본 구성 및 배포 환경

우리 홈페이지는 고비용의 별도 웹 서버를 구매하지 않고, 최신 클라우드 기술을 활용하여 관리 리소스와 비용을 최소화하도록 설계되었습니다.

* **화면 구현 (프론트엔드)**: 화면 개발에는 **React**와 **TypeScript**가 사용되었습니다. 화면이 매우 빠르게 반응하고, 미려한 UI 요소와 동적인 애니메이션을 지원합니다.
* **소스 코드 관리 (GitHub)**: 개발에 필요한 모든 소스 코드는 **GitHub(깃허브)** 저장소에서 보관 및 관리합니다.
* **자동 배포 및 호스팅 (Vercel)**: 개발자가 깃허브에 코드를 업데이트(Push)하면, **Vercel(버셀)** 호스팅 플랫폼이 변경된 코드를 감지하여 실제 인터넷 사이트에 자동으로 빌드하고 배포(CI/CD)를 완료합니다.

---

## 2. 데이터베이스 및 외부 연동 API

홈페이지 내에 들어가는 다양한 데이터와 인공지능, 지도 서비스는 검증된 글로벌 플랫폼을 연동하여 작동합니다.

* **클라우드 데이터베이스 (Supabase)**: 
  * 회원 관리(로그인/회원가입) 및 게시글, 리포트, 뉴스 기사 등의 데이터를 안전하게 저장하는 클라우드 데이터베이스입니다.
* **구글 지도 API (Google Maps API)**:
  * 홈페이지 내에서 여행 추천 가이드 맵과 경로를 지도 위에 실시간으로 표시해 줍니다.
* **구글 인공지능 API (Google Gemini API)**:
  * 챗봇이 사용자의 질문에 답을 하거나 최신 정보 요약문을 생성할 때 지능적인 답변을 만들어 주는 두뇌 역할을 합니다.

---

## 3. 핵심 자동화 기능

투어리즘 인사이트 홈페이지에는 단순한 웹페이지를 넘어, 백그라운드에서 자동으로 일을 처리하는 스마트한 시스템이 탑재되어 있습니다.

### 3.1 AI 가이드 챗봇
* **기능**: 사용자의 질문(예: "제주도 2박 3일 힐링 코스 추천해 줘")을 이해하고, 최적화된 여행 일정 및 관광 명소를 실시간으로 가이드해 주는 인공지능 비서입니다.
* **보안 업그레이드**: 기존에는 브라우저에서 직접 AI 키를 호출하여 해킹 봇에 의해 프로젝트가 정지되는 문제가 있었습니다. 이번 조치를 통해 **Vercel 서버 내부를 거쳐 호출(프록시 방식)**하도록 전면 수정하여, API 키가 외부로 절대 노출되지 않도록 철저히 보안을 강화했습니다.

### 3.2 뉴스 업데이트 에이전트 (AutoAgent)
* **기능**: 매일 또는 정기적으로 인터넷을 돌아다니며 최신 네이버 뉴스 및 구글 트렌드를 모니터링하는 **자동화 정보 수집 로봇**입니다.
* **작업 흐름**: 
  1. 수집한 기사 중 투어리즘 인사이트(관광 트렌드)에 적합한 기사를 선별합니다.
  2. 기사 본문을 분석하여 핵심 요약본을 자동으로 작성합니다.
  3. 최종 정제된 고품질의 기사를 우리 Supabase 데이터베이스에 자동으로 삽입하여, **뉴스룸 대시보드 게시판에 자동으로 퍼블리싱**합니다.

---

## 4. 보안 및 환경 변수 운영 정책

프로젝트에 사용되는 중요 인증 비밀키(DB 접속 정보, Gemini API 키 등)는 프로그램 소스코드 내부에 적어두지 않습니다.

1. **로컬(개발자 PC) 환경**: `.env`라는 설정 파일에 비밀키를 따로 저장하여 개발을 진행합니다. 이 파일은 GitHub 저장소에 올라가지 않도록 설정되어 있어 외부인이 볼 수 없습니다.
2. **배포(실제 사이트) 환경**: Vercel 플랫폼 관리자 페이지에 환경 변수(Environment Variables) 형태로 비밀키를 주입하여 가동하므로, 일반 웹 브라우저나 외부에서는 절대 탈취가 불가능합니다.

---

## 5. 주요 기술 용어 설명 (Glossary)

안내서에 작성된 주요 기술 및 전문 용어에 대한 쉬운 설명입니다.

* **프론트엔드 (Frontend)**: 웹사이트에서 사용자가 눈으로 보고 직접 클릭하며 상호작용하는 모든 화면 영역(웹 브라우저 환경)을 의미합니다.
* **React (리액트)**: 웹 브라우저 화면을 빠르고 효율적으로 만들기 위해 페이스북(메타)에서 개발한 화면 UI 전용 개발 도구입니다.
* **TypeScript (타입스크립트)**: 오타나 잘못된 명령어로 인한 실행 오류를 코드 작성 단계에서 미리 발견하여 방지해 주는 일종의 안전장치가 추가된 프로그래밍 언어입니다.
* **GitHub (깃허브)**: 전 세계 개발자들이 소스 코드의 전체 역사(버전)를 저장하고 백업하며 협업하는 안전한 온라인 코드 보관소입니다.
* **Vercel (버셀)**: 직접 복잡한 웹 서버 장비를 사고 관리할 필요 없이, 소스 코드만 올려두면 자동으로 전 세계 사용자가 볼 수 있는 웹서비스로 띄워주는 클라우드 호스팅 서비스입니다.
* **CI/CD (지속적 통합 및 지속적 배포)**: 개발자가 코드를 업로드하면 자동으로 빌드, 에러 테스트를 거쳐 인터넷 실제 사이트에 실시간으로 배포해 주는 자동 배포 파이프라인 시스템입니다.
* **Supabase (수파베이스)**: 회원 가입, 로그인, 게시판 글 저장 등 대규모 서버 프로그래밍이 필요한 작업을 마우스 클릭 몇 번으로 쉽게 가져다 쓸 수 있도록 해주는 클라우드 서버리스 백엔드 서비스입니다.
* **API (Application Programming Interface)**: 구글 지도나 AI 등 서로 다른 프로그램이나 서비스들이 서로 대화하고 기능과 데이터를 안전하게 주고받을 수 있도록 규정된 약속 통로입니다.
* **프록시 (Proxy) 방식 (서버 중개)**: 사용자의 웹 브라우저가 최종 AI 서버(Gemini)에 직접 요청하지 않고, 중간의 가상 중개인(Vercel 서버)을 한 단계 거쳐서 요청을 대리 전달하는 방식입니다. 이 과정에서 사용자 화면 뒤편에 있는 비밀번호(API 키)가 노출되지 않도록 감춰주는 핵심 보안 기법입니다.
* **환경 변수 (Environment Variables)**: 비밀번호나 중요 인증 키와 같이 소스코드 파일 내부에 텍스트로 적어두면 해킹 위험이 있는 정보를 코드와 분리하여 컴퓨터 운영 시스템에 안전하게 따로 기록해 두고 불러와 쓰는 방식을 말합니다.

