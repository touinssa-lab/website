# [TINS-Daily Update Standard]
## AI 트렌드&뉴스 대시보드 업데이트 매뉴얼 (v1.8, 2026-05-21)

> [!NOTE]
> **매뉴얼 관리 규칙**: 본 매뉴얼은 업데이트 프로세스 변경 시 즉시 수정하며, 버전 표기는 `(v차수.판수, YYYY-MM-DD)` 형식을 엄격히 준수합니다.

이 문서는 투어리즘인사이트 홈페이지 > Signal > AI 트렌드&뉴스 페이지의 매일 업데이트 작업(AI Hot Keyword 및 AI Hot News)의 표준 절차를 규정합니다.

---

### 0. 업데이트 전 필수 단계: 데이터 백업 및 동기화 (Data Safety)
> [!IMPORTANT]
> **데이터 유실 방지를 위한 2중 안전 장치 (Dual-Sync Strategy)**
> 1. **이전 데이터 백업**: 새로운 날짜의 데이터를 로컬 파일(`.ts`)에 업데이트하기 전, 반드시 기존 로컬에 있던 데이터(어제자)가 Supabase DB로 마이그레이션되었는지 확인합니다.
> 2. **즉시 동기화 (권장)**: 당일 데이터를 로컬 파일에 반영한 직후, **해당 데이터를 즉시 Supabase DB에도 `upsert` 하여 동기화**합니다.

*   **원인**: 시스템은 당일 데이터는 로컬 파일을 참조하고, 과거 데이터는 DB를 참조합니다. 백업 없이 로컬 파일을 덮어쓰면 이전 데이터는 영구 유실됩니다.
*   **방법**: 로컬 파일(`aiHotKeywords.ts`, `naverNewsData.ts` 등)의 최신 내용을 바탕으로 Supabase의 각 테이블(`news_trends_...`)에 즉시 `upsert`를 수행하여 데이터를 영구 보존 처리합니다.
*   **기대 효과**: 업데이트 시점에 DB 저장을 병행하면, 다음 날 업데이트 시 백업 단계를 실수로 누락하더라도 아카이브 데이터 공백이 발생하지 않습니다.

---

### 1. Today Top Rankings (키워드 랭킹)
관광 산업의 실시간 흐름을 파악하기 위한 구글 트렌드 기반 섹션입니다.

*   **데이터 수집 (Collection) - [사용자 수동 수집]**
    *   **방법**: 사용자가 직접 구글 트렌드 접속 후 데이터를 엑셀로 추출하여 제공.
    *   **파일 저장 경로**: `\Web\scratch\trends\YYYY-MM-DD.xlsx` (하이픈 포함)
    *   **출처**: Google Trends 실시간 급상승 검색어 및 관련 주제.
    *   **섹션**: 여행, 관광, 축제, 행사, 공연, 호텔, 항공, 맛집, 크루즈 (총 9개).
    *   **검색조건**: 트렌드 위치: South Korea(대한민국) / 기간: 지난 24시간 / 검색대상: 웹 검색
    *   **수량**: 섹션별 1위 ~ 50위까지 키워드 확보.
*   **선정 및 가공 (Selection)**
    *   **정규화**: '미식' → '맛집', '공항' → '항공' 등 시스템 표준 명칭으로 치환.
    *   **필터링**: 관광과 무관한 IT/기술적 용어(예: 에이전틱 AI 등)는 제외.
*   **시스템 반영 (Display)**
    *   **DB**: `news_trends_keywords` 테이블에 저장.
    *   **UI**: 여행, 관광, 축제, 행사, 공연, 호텔, 항공, 맛집, 크루즈 섹션 순으로 10개 단위로 자동 롤링 처리. 앞 섹션이 10개 단위로 끝나지 않을 경우 나머지 부족한 숫자는 다음 섹션 순위로 채울 것. (예시: 항공 1~6위 + 맛집 1~4위 합쳐서 한 슬라이드 구성)

---

### 2. AI Selected Insights (핵심 인사이트)
당일 가장 중요한 관광 이슈를 AI 에이전트가 선별하여 요약하는 섹션입니다.

*   **데이터 수집 (Collection) - [자동 수집 및 수동 엑셀 바이패스 지원]**
    *   **대상**: 당일 Today Top Rankings에서 수집된 세자릿수의 키워드.
    *   **분석**: AI가 여러 소스를 대조하여 가장 주목할 만한 인사이트 도출.
    *   **수동 엑셀 우회 경로**: `\Web\scratch\insights\YYYY-MM-DD.xlsx` (하이픈 포함)
        *   *설명*: 해당 경로에 오늘 자 엑셀 파일이 존재하면 AI 생성(Gemini API)을 스킵하고 엑셀 분석값을 즉시 로드하여 DB화합니다.
        *   *지원 컬럼*: `keyword`(키워드/주제), `category`(카테고리/분류), `reason`(내용/분석)
        *   *작성 팁*: `reason` 컬럼을 비우는 대신 `fact_check`(팩트체크) 컬럼과 `implication`(산업영향/시사점) 컬럼으로 쪼개서 작성해도 시스템이 자동으로 병합하여 반영합니다. `type`(타입/유형) 컬럼에 `analysis`/`unique`를 지정할 수도 있습니다 (생략 시 기본 2:2 자동 배분).
*   **선정 기준 (Selection)**
    *   단순한 사실(Fact) 나열을 넘어 산업적 영향력(Impact)이 포함된 키워드 선정.
    *   **매일 딱 4개의 핵심 인사이트로 고정** (화면 구성에 맞춰 `analysis` 2개, `unique` 2개의 균형 비율로 생성).
    *   **중복 배제 및 예외**: 이전에 선정되었던 키워드는 가급적 제외하여 다양성을 확보하되, 해당 키워드의 순위가 계속 높게 유지되는 핵심 이슈일 경우 재선정 가능.
*   **시스템 반영 (Display)**
    *   **DB**: `news_trends_insights` 테이블에 저장.
    *   **UI**: 좌측 대형 하이라이트 카드에 노출. 
    *   **카드 구성요소**: 
        1. 키워드 제목
        2. 키워드 분야
        3. 아이콘 (알고리즘 매칭: 급상승 키워드, 독특한 키워드, 시즌 핫이슈 등)
        4. 팩트 체크를 통한 선정 사유

---

### 3. AI Hot News (분야별 뉴스 피드)
관광 및 기술 분야의 최신 소식을 제공하는 피드 섹션입니다.

*   **데이터 수집 (Collection) - [자동 수집 및 수동 엑셀 바이패스 지원]**
    *   **검색 경로**: 반드시 [네이버 뉴스 메인](https://news.naver.com/)에 접속하여 우측 상단의 **검색(돋보기) 버튼**을 통해 검색할 것.
    *   **수동 엑셀 우회 경로**: `\Web\scratch\news\YYYY-MM-DD.xlsx` (하이픈 포함)
        *   *설명*: 해당 경로에 오늘 자 엑셀 파일이 존재하면 실시간 크롤러 작동을 우회하고 엑셀의 수동 뉴스 리스트를 즉시 로드하여 요약(Gemini) 및 DB 연동을 처리합니다.
        *   *시트 구성*: `tourism`(또는 `관광`) 및 `ai_data`(또는 `IT_AI` / `데이터`) 2개 시트
        *   *지원 컬럼*: `title`(제목), `url`(링크), `press`(언론사), `image_url`(이미지 - 생략 가능)
    *   **검색어(키워드) 최적화 기준**: 단일 키워드 검색 시 발생하는 무관한 기사(정치, 연예, 스포츠, 일반 IT 등) 수집을 방지하기 위해, 반드시 도메인이 결합된 복합 키워드를 사용할 것.
        *   **Tourism News**: "관광 산업", "여행 트렌드", "관광 축제", "관광 행사", "호텔 비즈니스", "항공 노선", "여행 소비", "크루즈 관광" 등 조합 검색.
        *   **AI & Data**: "관광 AI", "여행 빅데이터", "스마트관광", "트래블테크", "관광 디지털전환" 등 도메인 한정 키워드 사용.
    *   **필터**: 수집 당일 날짜로 등록된 뉴스만 수집 (검색 결과에 '1일 전' 이상으로 표기된 경우 제외).
    *   **URL 동시 수집 (Pairing)**: 검색 결과 페이지에서 후보 기사를 선별할 때, **제목과 네이버 뉴스 URL을 반드시 한 쌍(Pair)으로 동시에 수집**할 것. 
    *   **재검색 금지**: 제목만 따로 저장한 후 나중에 다시 검색하여 주소를 찾는 방식은 오차가 발생하므로 절대 금지하며, 최초 수집 시 확보된 URL을 통해 해당 기사 페이지로 직행할 것.
*   **가공 및 선정 (Selection)**
    *   **필수 조건**: 검색 결과에 **'네이버뉴스'** 버튼이 반드시 존재해야 함 (없을 경우 선정 제외).
    *   **중복 주제 배제 (Topic Uniqueness)**: 언론사가 다르더라도 동일한 보도자료나 사건을 바탕으로 한 중복 주제의 기사는 가장 대표성이 높은 기사 **하나만 선정**할 것.
    *   **수량 유연성 (Quantity Flexibility)**: 양질의 유니크한 기사가 부족하여 섹션별 6개를 채우기 어려운 경우, 중복 기사로 수량을 채우지 말고 **수집 가능한 기사 개수까지만 업데이트**할 것 (최대 6개).
    *   **기사 제목 (Zero-Tolerance)**: **원문 기사의 제목을 글자 하나, 띄어쓰기, 문장 부호 하나까지 100% 동일하게 사용**할 것.
        *   **절대 금지**: 제목 요약, 단어 추가/삭제, 인위적 창작, 마침표나 쉼표 등 문장 부호 임의 수정 엄금.
        *   **자가 검증**: AI 에이전트는 저장 직전, 추출한 제목을 네이버 검색창에 다시 입력하여 검색 결과 최상단에 동일 기사가 노출되는지 '크로스 체크'를 반드시 수행해야 함.
    *   **기사 링크 (2026-05-11 개정)**: 네이버 뉴스 주소(`n.news.naver.com`)를 그대로 '언론사 기사원문 읽기' 링크로 사용함. (작업의 효율성 및 링크 연결의 정확성을 확보하기 위해 개별 언론사 사이트 2차 추출 단계는 생략함)
    *   **산업군 매칭**: 투어리즘인사이트 10대 산업군별 연관 키워드(총 50개)와 매칭되는 기사 우선 선정.
        1.  **정책/행정**: 관광 거버넌스, 무비자/전자비자, 관광 수용태세, 국가 브랜딩, 관광 특구 지정
        2.  **경제/지역**: 관광 바우처, 지역 경제 승수효과, DMO, 인바운드 소비 전략, 관광 일자리 창출
        3.  **사회/인구**: 로컬리즘, 오버투어리즘 대응, 무장애 관광, 생활 인구, 시니어 액티브 투어
        4.  **테크/디지털**: 스마트 관광도시, AI 여행 컨시어지, 실감형 콘텐츠, 관광 빅데이터 분석, 트래블 테크 플랫폼
        5.  **환경/기후**: 탄소중립 여행, 재생 관광, 에코 투어리즘, 그린 모빌리티, 노 플라스틱 투어
        6.  **의료/웰니스**: 메디컬 투어리즘, 산림/해양 치유, 웰니스 스테이, 멘탈 헬스 리트릿, K-뷰티 투어
        7.  **미디어/콘텐츠**: 필름 투어리즘, 인스타그래머블 스팟, 관광 스토리텔링, 팝컬처 성지순례, 트래블 로그
        8.  **모빌리티/교통**: 관광 MaaS, 자율주행 관광 셔틀, 라스트마일 이동성, Scenic Route, 스마트 환승 시스템
        9.  **유통/식음료**: 가스트로 투어, 로컬 팝업 스토어, 지역 특산물 브랜딩, 전통주 양조장 투어, 제로웨이스트 마켓
        10. **노동/교육**: 워케이션, 디지털 노마드, 에듀테인먼트 투어, 한달 살기, 관광 리터러시 교육
*   **이미지 및 요약 (Processing)**
    *   **기사 요약**: 원문의 내용을 함축한 **4문장 요약** (단순 첫 문장 나열 엄금).
        *   **내부 로직**: LLM의 짜임새 있는 문장 구성을 보장하기 위해 `[기]`, `[승]`, `[전]`, `[결]` 접두어를 사용하여 4문장 구조 검증을 내부적으로 수행합니다.
        *   **최종 표출 (개정)**: 검증 완료 후, **`[기]`, `[승]`, `[전]`, `[결]` 등 대괄호 태그는 완벽하게 자동 소거(Strip)하여 순수하고 품격 있는 하나의 완성형 4문장 문단으로만 DB 및 화면에 반영합니다.
    *   **이미지 처리 (협업)**:
        1. **AI 에이전트 (링크 제공 및 폴더 생성)**:
            *   `public/images/news/` 하위에 **반드시 하이픈(-)을 포함한 YYYY-MM-DD 형식**의 폴더를 생성함. (예: `2026-05-11`)
            *   **사용자가 기사 이미지를 수집할 수 있도록, 최종 선정된 모든 기사의 [제목 - 네이버 뉴스 URL] 리스트를 채팅창에 일목요연하게 출력하여 전달함.**
        2. **사용자 (이미지 파일명 규칙)**: AI가 제공한 링크에 접속하여 해당 기사 페이지의 이미지를 아래 규정에 맞춰 해당 날짜 폴더에 수동 저장함.
            *   **Tourism News**: `article_1.jpg`, `article_2.jpg` ...
            *   **AI & Data News**: `article_ai_1.jpg`, `article_ai_2.jpg` ...
            *   (파일 형식은 **JPG**로 통일)
*   **시스템 반영 (Display)**
    *   **수량**: **각 섹션별(Tourism, AI & Data) 4~6개**의 뉴스 선정.
    *   **DB**: `news_trends_articles` 테이블에 저장.
    *   **기본값**: Tourism News 탭이 항상 먼저 노출되도록 설정.

---

### 4. 시스템 최종 동기화 (Final Synchronization)
모든 데이터 업데이트가 완료된 후, 사용자가 접속했을 때 당일 데이터가 즉시 노출되도록 시스템 설정을 동기화해야 합니다.

1.  **날짜 업데이트 (Date Update)**
    *   **대상 파일**: `src/pages/NewsRoom.tsx`
    *   **수정 항목**: 
        *   `selectedDate`의 기본값(useState)을 당일 날짜로 변경.
        *   `useQuery` 내의 로컬 데이터 매칭 조건문(`if (selectedDate === 'YYYY-MM-DD')`)을 당일 날짜로 변경.
        *   아카이브 달력의 `max` 값을 당일 날짜로 업데이트.
2.  **데이터 품질 검수**: 썸네일 이미지가 깨지거나 요약이 부자연스러운 기사는 즉시 교체.
3.  **최종 확인 및 육안 검증**:
    *   AI 에이전트는 직접 로컬 환경(`http://localhost:8080`)에 접속하여 데이터 렌더링 상태를 최종 확인해야 함.
    *   브라우저를 새로고침하여 상단 'ARCHIVE DATE'와 'Today Top Rankings'의 날짜가 오늘 날짜로 정확히 표시되는지, 뉴스 요약(4문장) 및 이미지가 정상 출력되는지 확인.

### 5. 자동화 예외 처리 및 수집 누락 시 대응 가이드 (v1.8 개정)
*   **중복 구동 및 단계별 스킵 로직 (Dual-Skip Logic)**:
    *   에이전트 구동 시 오늘 자 데이터가 DB에 이미 적재되어 있다면 불필요한 구동을 중단하고 즉각 종료합니다.
    *   만약 일부 데이터(키워드, 인사이트, 뉴스)가 누락되어 작동을 시작한 경우, **이미 DB에 존재하는 단계는 수집 및 DB 연동을 똑똑하게 생략(Bypass)하고 비어있는 단계만 가동**하여 업데이트합니다.
    *   수동으로 강제 재실행이 필요할 때는 실행 인수 뒤에 `--force` 플래그를 추가하거나 대시보드 수동 실행 버튼을 클릭하여 중복 감지 제한을 우회할 수 있습니다.
*   **원칙**: 시스템 문제, PC 오프라인, 고의적/실수 등으로 특정 날짜의 자동 업데이트가 누락된 경우, 과거 데이터를 억지로 메우거나 훼손하지 않고 **해당 날짜의 Supabase 데이터는 공백**으로 둡니다.
*   **프론트엔드 대응 UI**:
    *   사용자가 아카이브 달력에서 데이터가 누락된 날짜를 선택하면, 개별 카드에 빈 화면이 뜨는 대신 **통합된 프리미엄 안내 카드("수집된 데이터가 없습니다")**가 렌더링됩니다.
    *   해당 안내 영역에는 독창적인 **Red 'X' 배지가 부착된 달력 아이콘**이 출력되며, 사용자가 즉시 정상적인 데이터 페이지로 돌아갈 수 있도록 **[최신 데이터 날짜로 이동]** 버튼이 함께 노출되어 안전하게 복구를 돕습니다.

---

### 6. 향후 개발 로드맵: AI 에이전트 통합 관제 센터 (Agent Ops)
*   **목적**: 보이지 않는 유령처럼 작동하는 백그라운드 에이전트들을 편리하게 시각적으로 감시하고 개별 제어하기 위함.
*   **구현 방향**:
    1.  **에이전트 테이블 추가 (`agent_status_logs`)**: 에이전트 구동 시작/단계별 실시간 로그/성공여부 기록.
    2.  **Admin UI 개발 (`src/pages/admin/Agents.tsx`)**:
        *   **실시간 구동 현황 위젯**: 준비 중(Idle), 작동 중(Running), 오류(Error) 등 네온 배지 표시.
        *   **터미널 콘솔 스트리밍**: 에이전트의 상세 로깅 텍스트를 웹 브라우저 내에서 직접 개발자 콘솔 창처럼 흘려줌.
        *   **수동 원클릭 강제 구동 버튼**: 버튼 터치 한 번으로 PC/서버에 명령을 전달해 즉시 재수집 가동 처리.
    3.  **데이터 품질 대시보드**: 오늘 수집된 데이터의 긍부정 스코어 비율, 글자 수, API 사용 비용 통계 분석 차트 제공.

---
**최종 수정일**: 2026-05-21 (v1.8 개정)
**관리자**: Tourism Insight AI Agent & USER

> [!CAUTION]
> 업데이트 작업 중 `NewsRoom.tsx`의 날짜를 당일로 변경하는 순간, 시스템은 로컬 파일을 우선적으로 조회하게 됩니다. 만약 이때 로컬 파일에 데이터가 비어있거나 구형 데이터가 들어있다면 서비스에 즉시 영향을 미치므로, 파일 저장과 코드 수정을 신중하게 동시 진행하십시오.

